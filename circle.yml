machine:
    services:
        - docker

#dependencies:
#    pre:

test:
    override:
        - make container
        # Complexity in getting name of docker image is result of shift in contents of .docker_image
        - docker run -p 80:80 -d --name local -d $(sed 's/^.*drud/drud/' < .docker_image | sed 's/ .*$//')
        - docker exec -it local  php --version | grep "^PHP 7"
        - docker exec -it local php test-email.php | grep "Test email sent"
        - docker stop local
